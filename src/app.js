import calculateTaxonLine from './model/taxonLine';
import Maybe from './lib/Maybe';

export class Checklist {

    constructor(text) {

        // convert each line to an array item
        // map each row into values
        //
    }
}
